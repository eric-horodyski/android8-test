import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'android8-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
